<idea-plugin>
    <id>org.jetbrains.compose.desktop.ide</id>
    <name>Compose Multiplatform for Desktop IDE Support</name>
    <vendor>JetBrains</vendor>
    <description>
        <![CDATA[
<h3>⚠️ This plugin is in maintenance mode</h3>
<p>The Compose Multiplatform for Desktop IDE Support plugin is no longer under active development.
Its functionality is being gradually integrated into the new <a href="https://plugins.jetbrains.com/plugin/14936-kotlin-multiplatform">Kotlin Multiplatform plugin</a>,
which already includes support for Compose Previews on macOS.</p>
<p>What this means for you:</p>
<ul>
    <li>On macOS: We recommend switching to the <a href="https://plugins.jetbrains.com/plugin/14936-kotlin-multiplatform">Kotlin Multiplatform plugin</a>, which now includes improved Compose Previews support.</li>
    <li>On Windows/Linux: You can continue using this plugin. It remains available and maintained for now, until equivalent support becomes available in the Kotlin Multiplatform plugin.</li>
</ul>
<p>No new features will be added, but we’ll address critical issues as needed during this transition.</p>
        ]]>
    </description>

    <!-- until-build is omitted, so we don't have to update it after each Intellij release -->
    <idea-version since-build="213"/>

    <!-- Product and plugin compatibility requirements -->
    <!-- https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html -->
    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.java</depends>
    <depends>com.intellij.gradle</depends>
    <depends>org.jetbrains.kotlin</depends>

    <extensions defaultExtensionNs="org.jetbrains.kotlin">
        <supportsKotlinPluginMode supportsK2="true" />
    </extensions>

    <incompatible-with>com.jetbrains.kmm</incompatible-with>
    <extensions defaultExtensionNs="com.intellij">
        <pluginReplacement implementation="org.jetbrains.compose.ComposePluginReplacement"/>

        <runLineMarkerContributor
                language="kotlin"
                implementationClass="org.jetbrains.compose.desktop.ide.preview.PreviewRunLineMarkerContributor"/>
        <deadCode implementation="org.jetbrains.compose.desktop.ide.preview.PreviewEntryPoint" />

        <runLineMarkerContributor
                language="kotlin"
                implementationClass="org.jetbrains.compose.web.ide.run.WebRunLineMarkerContributor"/>
        <runConfigurationProducer
                implementation="org.jetbrains.compose.web.ide.run.WebRunConfigurationProducer"/>

        <toolWindow
                factoryClass="org.jetbrains.compose.desktop.ide.preview.PreviewToolWindow"
                id="Desktop Preview" doNotActivateOnStart="true"
                anchor="right" />

        <lang.inspectionSuppressor language="kotlin" implementationClass="org.jetbrains.compose.inspections.ComposeSuppressor"/>

        <notificationGroup id="Compose MPP Notifications" displayType="BALLOON"/>

        <editorFloatingToolbarProvider implementation="org.jetbrains.compose.desktop.ide.preview.PreviewFloatingToolbarProvider"/>
    </extensions>

    <actions>
        <group id="Compose.Desktop.Preview.Editor.Toolbar">
            <action class="org.jetbrains.compose.desktop.ide.preview.RefreshOrRunPreviewAction"/>
        </group>
    </actions>
</idea-plugin>
